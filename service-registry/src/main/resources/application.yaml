server:
  port: 8761

eureka:
  client:
    register-with-eureka: false  # Kendini kaydetmesin
    fetch-registry: false        # Ba?ka servisleri çekmesin
    service-url:
      defaultZone: http://localhost:8761/eureka/

spring:
  application:
    name: service-registry